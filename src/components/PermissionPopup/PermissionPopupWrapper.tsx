// components/PermissionPopupWrapper.tsx
"use client";

import PermissionPopup from './PermissionPopup';

const PermissionPopupWrapper: React.FC = () => {
  return <PermissionPopup />;
};

export default PermissionPopupWrapper;
